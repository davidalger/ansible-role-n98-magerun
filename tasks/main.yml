- name: install n98-magerun
  get_url:
    url: "{{ n98_magerun.download_url }}"
    dest: /usr/local/bin/{{ n98_magerun.install_name }}
    mode: 0755

- name: link {{ n98_magerun.shortcut_name }} shortcut
  file:
    path: /usr/local/bin/{{ n98_magerun.shortcut_name }}
    src: /usr/local/bin/{{ n98_magerun.install_name }}
    state: link
